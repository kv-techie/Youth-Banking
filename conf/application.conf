# Youth Banking Application Configuration

# Secret key for Play Framework
play.http.secret.key = "changeme-for-production-use-only"

# Application configuration
play.i18n.langs = ["en"]

# Allowed hosts
play.filters.hosts {
  allowed = ["."]
}

# CORS configuration (if needed)
play.filters.cors {
  pathPrefixes = ["/"]
  allowedOrigins = null
  allowedHttpMethods = null
  allowedHttpHeaders = null
}

# Database configuration (when you add real DB)
# db.default.driver = org.postgresql.Driver
# db.default.url = "jdbc:postgresql://localhost/youthbank"
